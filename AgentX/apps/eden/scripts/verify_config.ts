import fs from 'fs'; import path from 'path'; import os from 'os'; const configPath = path.resolve(os.homedir(), '.eden_data/platform_configs.json'); try { const data = fs.readFileSync(configPath, 'utf8'); const configs = JSON.parse(data); const feishuConfig = configs.find(c => c.platform === 'feishu'); if (feishuConfig) { console.log('Feishu Config Found:'); console.log('AgentID:', feishuConfig.agentId); console.log('Enabled:', feishuConfig.enabled); console.log('AppID:', feishuConfig.config.appId); console.log('AppSecret:', feishuConfig.config.appSecret ? feishuConfig.config.appSecret.substring(0, 4) + '******' : 'MISSING'); } else { console.log('No Feishu config found'); } } catch (e) { console.error('Error reading config:', e.message); }
